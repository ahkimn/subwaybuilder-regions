import type { RegionsSettings } from './types';

export const DEFAULT_REGIONS_SETTINGS: RegionsSettings = {
  showUnpopulatedRegions: false, // By default, we should hide any unpoulated regions as they have no gameplay effect.
};
